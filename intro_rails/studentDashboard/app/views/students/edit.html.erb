<!doctype html>
<html lang="en">
    <head>
        <title>Title</title>
        <!-- Required meta tags -->
        <meta charset="utf-8">
        <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">

        <!-- Bootstrap CSS -->
        <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.3.1/css/bootstrap.min.css" integrity="sha384-ggOyR0iXCbMQv3Xipma34MD+dH/1fQ784/j6cY/iJTQUOhcWr7x9JvoRxT2MZw1T" crossorigin="anonymous">
    </head>
    <body>
        <h1>Editing</h1>
        <form action="/update/<%= @dojo.id %>/<%= @student.id %>" method="post">
            <input type="hidden" name="authenticity_token" value="<%= form_authenticity_token %>">
            <input type="hidden" name="_method" value="patch">
            <span>First Name: </span><input type="text" name="student[first_name]" value="<%= @student.first_name %>">
            <span>Last Name: </span><input type="text" name="student[last_name]" value="<%= @student.last_name %>">
            <span>Email: </span><input type="text" name="student[email]" value="<%= @student.email %>">
            <select name="student[dojo]">
                <% @d.each do |dojo|%>
                <option value="<%= @dojo.id %>"><%= dojo.branch %></option>
                <% end %>
            </select>
            <input type="submit" value="Update">
        </form>
        <u><a href="/dojoDashboard/<%= @dojo.id %>">Go Back</a></u>
        <u><a href="/<%= @dojo.id %>/<%= @student.id %>/students/new"><%= @student.first_name %> <%= @student.last_name %></a></u>
        <!-- Optional JavaScript -->
        <!-- jQuery first, then Popper.js, then Bootstrap JS -->
        <script src="https://code.jquery.com/jquery-3.3.1.slim.min.js" integrity="sha384-q8i/X+965DzO0rT7abK41JStQIAqVgRVzpbzo5smXKp4YfRvH+8abtTE1Pi6jizo" crossorigin="anonymous"></script>
        <script src="https://cdnjs.cloudflare.com/ajax/libs/popper.js/1.14.7/umd/popper.min.js" integrity="sha384-UO2eT0CpHqdSJQ6hJty5KVphtPhzWj9WO1clHTMGa3JDZwrnQq4sF86dIHNDz0W1" crossorigin="anonymous"></script>
        <script src="https://stackpath.bootstrapcdn.com/bootstrap/4.3.1/js/bootstrap.min.js" integrity="sha384-JjSmVgyd0p3pXB1rRibZUAYoIIy6OrQ6VrjIEaFf/nJGzIxFDsf4x0xIM+B07jRM" crossorigin="anonymous"></script>
    </body>
</html>